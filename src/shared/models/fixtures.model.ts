export class FixturesModel {
    constructor(
        public events?: boolean,
        public lineups?: boolean,
        public statistics_fixtures?: boolean,
        public statistics_players?: boolean,
    ) {
    }
}
